package open_closed.bad;

public class SalaryPersistence {
    public void saveToFile(int salary) {
        // Writes the salary to file
    }

    public void saveToDatabase() {
        // Saves the salary to database
    }

    // what if we want to add a new feature and to save salary to cloud?
}
